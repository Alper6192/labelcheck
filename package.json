{
  "name": "label-check-florence",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "download:model": "node scripts/download-model.mjs",
    "copy:runtime": "node scripts/copy-ort-assets.mjs",
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/main.js && node --check src/editor.js && node --check src/florence-worker.js && node --check src/profile-engine.js && node --check src/ocr-entries.js && node --check src/comparison.js && node --check src/barcode.js"
  },
  "dependencies": {
    "@huggingface/transformers": "3.8.1",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "vite": "7.1.7"
  }
}
