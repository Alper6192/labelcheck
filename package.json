{
  "name": "label-check-paddleocr",
  "private": true,
  "version": "0.6.9",
  "type": "module",
  "scripts": {
    "dev": "npm run prepare:runtime && vite --host 0.0.0.0",
    "prepare:runtime": "node scripts/copy-ort-assets.mjs && node scripts/copy-model-assets.mjs",
    "build": "npm run prepare:runtime && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@paddleocr/paddleocr-js": "0.4.2",
    "xlsx": "0.18.5",
    "jsqr": "1.4.0",
    "onnxruntime-web": "1.22.0"
  },
  "devDependencies": {
    "vite": "6.4.1"
  }
}
