# Patch 0.5.3

Inhalt in den bestehenden Repository-Ordner kopieren und vorhandene Dateien ersetzen.

Dieser Patch enthält absichtlich keine `public/config/label-profiles.json`. Eigene Profile bleiben erhalten.

Der Scanner bleibt unverändert. Nur der Profileditor nutzt ab dieser Version eine einzelne Hauptfenster-Instanz ohne Worker-/Timeout-Fallback.
